// Built-in functions from the C language

char* sprintf(char*, const char*, ...);
char* printf(const char*, ...);

// Dolphin

void OSReport(const char* msg, ...);

// Unknown functions

void fn_80191C1C(unsigned char*, int);
int fn_80183894();
